{% extends 'base.html.twig' %}

{% block title %}ReflexologieCôtière | Edit Session{% endblock %}

{% block body %}
    <div class="admin-crud-header">
        <a href="{{ path('admin_dashboard') }}" class="dashboard-back-btn">← Retour au tableau de bord</a>
    </div>
    <div class="admin-session-edit">
        <h1>Modifier la séance</h1>
        {{ include('session_crud/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <div style="text-align:center;">
            <a href="{{ path('app_session_crud_index') }}">Retour à la liste</a>
            {{ include('session_crud/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
