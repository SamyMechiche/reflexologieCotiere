{% extends 'base.html.twig' %}

{% block title %}Session{% endblock %}

{% block body %}
    <h1>Session</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ session.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ session.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ session.description }}</td>
            </tr>
            <tr>
                <th>Duration_minutes</th>
                <td>{{ session.durationMinutes }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ session.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_session_crud_index') }}">back to list</a>

    <a href="{{ path('app_session_crud_edit', {'id': session.id}) }}">edit</a>

    {{ include('session_crud/_delete_form.html.twig') }}
{% endblock %}
