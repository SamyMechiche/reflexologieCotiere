{# Only add if this is used as a standalone page #}
{{ form_start(form, {'attr': {'class': 'admin-session-form'}}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
