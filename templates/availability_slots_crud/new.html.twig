{% extends 'base.html.twig' %}

{% block title %}New Availability Slot{% endblock %}

{% block body %}
    <div class="admin-crud-header">
        <a href="{{ path('admin_dashboard') }}" class="dashboard-back-btn">← Retour au tableau de bord</a>
    </div>
    <h1>Create new AvailabilitySlots</h1>

    {{ include('availability_slots_crud/_form.html.twig') }}

    <a href="{{ path('app_availability_slots_crud_index') }}">back to list</a>
{% endblock %}
